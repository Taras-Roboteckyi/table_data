import { nanoid } from 'nanoid';

import AddButton from '../AddButton/AddButton';

export function AddLineMatrix() {
  //Створюєм новий рядок для матриці

  return <tfoot>{/* <tr>{dataLine.length > 0 && <AddButton addLine={newLine} />}</tr> */}</tfoot>;
}
